import React from 'react';
// import PropTypes from 'prop-types';
import SearchField from 'custom-fields/SearchFields'

// FormSearch.propTypes = {

// };

function FormSearch(props) {
    return (
        <div>
            <SearchField />
        </div>
    );
}

export default FormSearch;